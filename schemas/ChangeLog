2006-01-02 Jan Kryl (version 1.2.0)

	* New element 'reportlevel' in create command and info response was
	added in schema nsset-1.2. Element's value influences level of
	technical checks performed on nsset.

	* Oprava typu registranta u domeny v updatu.

	* Pridani "object-specific protocol extensionu" do schemat objektu.
	Je to prikaz pro zaslani authInfa klientovi (soucast noveho zpusobu
	transferu).

	* Vybudovani zazemi pro ruzna rozsireni EPP ne na urovni existujicich
	prikazu ale na urovni protokolu. Tato rozsireni budou definovat nove
	operace nad objekty, ktere nebyly definovany v EPP standardu.
	Rozsireni maji podobnou strukturu jako standardni prikazy (napr.
	obsahuji clTRID udaj). Tento rozsirujici framework je definovan ve
	schematu fred-1.0.xsd.

	* Pridani "object-specific" zprav do schemat objektu, zatim je to
	jen zprava o probehlem transferu.

	* Zruseni statusu nastavovanych klientem a tim padem i zruseni
	mechanismu pro jejich nastaveni.

	* Prejmenovani ssn elementu na ident. Element ssn je i nadale
	pripustny, ale je oznacen jako "obsolete" a v budoucnu bude odebran.
	Vyznam a struktura tagu zustala zachovana.

	* Zpovinneni technickeho kontaktu pri vytvareni nssetu a zrovna tak
	v odpovedi na info.

	* ExDate u domeny uz neni typ datetime ale pouze date.

	* Povyseni verze u vsech schemat, ktere se zmenily od verze 1.1.1
	mod_eppd (vsechny krome epp, eppcom, enumval a noveho schematu fred).

	* Z povineni authInfo elementu pri transferech objektu. Nepredstavuje
	az tak velkou zmenu, do ted bylo authInfo vyzadovano serverem, ted
	je vyzadovano na urovni schemat.

	* Obecnejsi typ authInfoType ktery mohl obsahovat jak plain text
	heslo, tak libolnou jinou autorizacni informaci (tag ext), byl
	zjednodusen. Od ted typ authInfoType obsahuje primo plain text heslo.

	* Pri infu slo zadat authInfo objektu, jako dodatecnou autorizaci
	k info prikazu. Vzhledem k tomu ze jsme to nepouzivali, byla vlastnost
	odebrana.

2006-09-26 Jan Kryl (version 1.1.1)

	* authInfo element pri vytvoreni objektu (domain, contact, nsset)
	je od ted nepovinny (V pripade neuvedeni je serverem automaticky
	vygenerovan).

	* pwAuthInfoType ma nyni omezenou delku (300 znaku). Toho bylo
	docileno novou definici typu ve fredcom a zmenou referenci v ostatnich
	schematech tak, aby neukazovaly do eppcom, ale na novy typ ve fredcom.
	Pro uzivatele schemat se nic nemeni.

2006-09-21 Jan Kryl (version 1.1.0)

	* Zalozeni tohoto ChangeLogu pro ucely dokumentace zmen do schemat.

	* Oficialni specifikace systemu rika ze v nssetu muze byt 1 az 10
	nameserveru. Do ted to bylo 1 az 9. Nekonzistence opravena, pocet
	zvysen o 1.

	* Pridani souboru fredcom-1.0.xsd, ktery je urcen pro definice
	typu vyuzivanych napric schematy. Obdoba eppcom-1.0.xsd, ktery
	jsme ale nemohli vyuzit, protoze je soucasti EPP standardu a nemuzeme
	jej tudiz modifikovat.

	* Zmena typu handlu nssetu a kontaktu. Puvodni typ clIDType z eppcom
	byl moc kratky. Byl nahrazen typem objIDType z fredcom. Nyni je
	maximalni delka identifikatoru az 63 znaku. Ostatni schemata byla
	pozmenena a prizpusobena teto zmene. Jako dusledek teto zmeny se
	rozlisuje uz typ identifikatoru objektu (contact, nsset) a
	identifikatoru klienta (clID, crID, trID, ...).

	* Byly vycisteny zakomentovane nepouzivane useky ze schemat, aby
	nematly ctenare.

